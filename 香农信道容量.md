# 雾海两座塔

## 寓言故事

雾海峡谷的两端各有一座灯塔。东塔守着王城，西塔守着边境。两塔之间没有道路，只有一片终年翻涌的灰雾。

过去，东塔用两种灯传递军情：白灯表示“开门”，黑灯表示“闭门”。每个夜刻亮一次，西塔照颜色抄录。可浓雾会让白灯看成黑灯，也会让黑灯看成白灯。天气稳定时，大约每十次错一次，而且每次出错看不出痕迹。

一次误读险些让敌军进城。国王召集工匠，要求造一盏“绝不出错的灯”。

铜匠把灯做大十倍，错误减少了一些，却没有消失。镜师把光束收得更窄，风一吹塔身，错得反而更多。军官提议每个命令重复三次，以多数颜色为准。

“白白白”确实比单个白灯可靠，但传递速度降成原来的三分之一。边境每天要发送的不只是开门与闭门，还有兵数、方位、粮草和时间。若每个符号都重复，消息还没传完，战事已经结束。

年轻抄码师遥织提出另一种方法。她不再逐字保护命令，而是先把许多命令合成一整段，再把这段意义改写为更长的灯序列。

她给四种命令安排了四个六灯序列：

```text
开门：000000
闭门：001111
增援：110011
撤退：111100
```

其中 0 表示黑灯，1 表示白灯。任意两个合法序列都相差多个位置。若雾只误读一盏，西塔看到的序列虽然不在表中，却通常仍离某个合法序列最近，于是可以纠正，而不只是发现错误。

军官问：“既然多加灯能纠错，为什么不让所有命令都用一千盏灯？”

遥织回答：“那当然可靠，却可能一天只传一个字。问题不是能否可靠，而是每次亮灯平均能带过去多少意义。”

她开始做一件看似荒唐的事：把越来越长的消息放在一起编码。十条命令共用一张码表，一百条命令共用更大的码表，一千条命令再用更长的灯序列。消息越长，码表中的合法序列越像散布在巨大夜空中的星座。

雾会把每个合法序列扰动到附近的一片区域。只要不同星座周围最常出现的“雾区”几乎不重叠，西塔看到受扰序列后，就能判断它来自哪颗星。偶尔出现极不寻常的大量误读，仍可能解错；但随着整段变长，这类异常的比例可以变得越来越小。

遥织发现，可靠性与速度之间存在一道看不见的界线。

当她试图在同样长度的灯序列里塞入过多命令时，合法星座变得太密。它们周围的常见雾区开始重叠，西塔无法判断收到的序列原属哪条消息。无论换什么巧妙码表，都不能让所有星座在有限空间中保持足够距离。

当她稍微降低传递速率时，却能在长序列空间里找到足够多、彼此分离的星座。序列越长，平均错误率越低，理论上可以低到任意要求。

国王听后说：“那你已经造出了绝不出错的灯。”

“没有，”遥织纠正，“任何有限长度仍有出错可能。我找到的是另一件事：只要每次亮灯承载的意义不超过雾海允许的极限，我们就能用越来越长、越来越好的码，把错误概率压到任意小；一旦超过那条极限，再聪明的码也无法让错误趋近于零。”

国王命她测出这条界线。

遥织先统计黑灯变白灯、白灯变黑灯的比例，又研究不同灯色出现频率是否会改变可辨认程度。若两种灯在雾中被对称地误读，她发现黑白均匀使用最有效；若白灯穿雾更强，最优比例就可能改变。界线不是由灯塔单方面决定，也不是由雾单方面决定，而由“发送什么”与“经过雾后还能辨认出多少”共同决定。

后来，边境军官为了炫耀速度，宣称自己每夜刻传两份独立命令，超过了遥织测得的极限。他确实让灯闪得更快，却开始频繁重传；有些错误甚至无法察觉。按成功送达的真实军情计算，速度反而更低。

遥织又提醒议会，那条界线只描述理想条件下的极限，不包括点灯延迟、码表查找、能源不足和敌人干扰。实际系统需要在时延、计算、能耗与可靠性之间取舍，通常只能接近界线，不能轻易贴着它运行。

多年后，两塔之间仍有雾，也仍偶有误码，但王城不再追求一盏神奇的无误之灯。人们学会在噪声中组织冗余、拉开消息之间的距离，并接受一个更深的事实：

噪声没有让可靠通信变得不可能。它只是为“每次使用这条道路，最多能可靠送达多少信息”画出了一条不可逾越的边界。

## 概念：香农信道容量

### 1. 核心定义

**香农信道容量（Shannon Channel Capacity）**是信息论中描述信道可靠传输极限的核心概念。它表示：在给定信道统计规律下，通过选择合适的输入分布和足够长的编码，每次信道使用能够以任意小错误概率传输的最大信息速率。

对离散无记忆信道，容量定义为：

```text
C = max P(X) I(X; Y)
```

其中：

- X 是信道输入。
- Y 是信道输出。
- P(X) 是可选择的输入分布。
- I(X; Y) 是输入与输出之间的互信息。
- C 的单位通常是 bit/次信道使用。

“无记忆”表示给定当前输入后，当前输出不依赖过去的输入输出。真实信道可能有记忆，此时容量需要更一般的定义。

### 2. 熵、条件熵与互信息

随机变量 X 的香农熵为：

```text
H(X) = −Σₓ P(x) log₂ P(x)
```

它衡量 X 的平均不确定性。

观察 Y 后，对 X 仍剩余的不确定性为条件熵：

```text
H(X | Y)
```

互信息定义为：

```text
I(X; Y) = H(X) − H(X | Y)
```

也可写为：

```text
I(X; Y)
= Σₓ,ᵧ P(x,y) log₂ [P(x,y) / (P(x)P(y))]
```

它表示观察 Y 后，关于 X 的不确定性平均减少了多少。若 X 与 Y 独立，则：

```text
I(X; Y) = 0
```

信道容量不是固定输入下的互信息，而是在所有允许输入分布中取最大值：

```text
C = max P(X) I(X; Y)
```

因为不同信道可能偏爱不同输入。例如某种符号更容易穿过信道时，均匀发送未必最优。

### 3. 二元对称信道

故事中的黑灯和白灯可抽象为二元符号 0 与 1。设每次传输以概率 p 被翻转：

```text
P(Y ≠ X) = p
P(Y = X) = 1 − p
```

这称为**二元对称信道（Binary Symmetric Channel，BSC）**。

二元熵函数为：

```text
h₂(p) = −p log₂ p − (1 − p) log₂(1 − p)
```

BSC 的容量为：

```text
C = 1 − h₂(p)
```

当 p = 0 时，信道无误码：

```text
C = 1 bit/次
```

当 p = 0.5 时，输出与输入完全无关：

```text
C = 0
```

例如 p = 0.1 时：

```text
h₂(0.1) ≈ 0.469
C ≈ 0.531 bit/次
```

这不表示某一次传输携带 0.531 个物理比特，而表示在长码字和最优编码的渐近意义下，每次信道使用最多能可靠承载约 0.531 个信息比特。

### 4. 编码率

设编码器把 k 个信息比特映射为长度 n 的信道输入序列，则编码率为：

```text
R = k / n
```

单位是 bit/次信道使用。

增加冗余意味着 n 大于 k，因此 R 下降。重复三次编码是最简单的冗余方案之一，但通常远非最优。更先进的纠错码把整段消息映射到结构化码字，使冗余同时保护多个信息比特。

可靠通信的目标是：在 R 尽可能高的同时，让块错误概率 P_e 尽可能低。

### 5. 有噪信道编码定理

香农有噪信道编码定理包含两个方向。

#### 可达性

对任意速率：

```text
R < C
```

都存在一系列长度不断增加的编码，使错误概率满足：

```text
P_e → 0，当 n → ∞
```

它不承诺某个有限码长绝对无误，而是说低于容量时，可以通过足够长、设计得当的编码把错误概率压到任意小。

#### 反面结论

若：

```text
R > C
```

则不存在一系列编码能让错误概率趋近于零。

在许多标准信道中，更强的结论成立：当速率超过容量时，随码长增长，正确解码概率会趋近于零。这称为强反定理。

因此，C 是可靠通信的分界线，不只是某种具体编码的性能指标。

### 6. 典型序列与“雾区”

为什么长块编码能接近容量？一个重要直觉来自典型集。

当 n 很大时，独立重复信源产生的绝大多数序列集中在典型集合中，其数量约为：

```text
2^(nH(X))
```

给定某个输入码字，信道输出大概率落在一个条件典型集合中，其有效规模约为：

```text
2^(nH(Y|X))
```

全部典型输出空间的规模约为：

```text
2^(nH(Y))
```

因此，可在输出空间中区分的码字数量大致受限于：

```text
2^(nH(Y)) / 2^(nH(Y|X))
= 2^(nI(X;Y))
```

也就是说，每次信道使用可区分的信息速率约为 I(X;Y)。再对输入分布优化，就得到容量 C。

故事中每个合法灯序列周围的“常见雾区”，对应条件典型输出集合。若这些区域严重重叠，接收者便无法可靠判断原始码字。

### 7. 汉明距离与纠错

对两个等长二进制序列，汉明距离 d_H 是它们不同位置的数量。

若一个码的最小汉明距离为 d_min，则它能够：

```text
检测最多 d_min − 1 个错误
纠正最多 floor((d_min − 1) / 2) 个错误
```

故事中合法灯序列彼此相差多个位置，因此单个翻转后仍可能通过最近邻规则恢复。

但香农定理并不要求所有优秀码都只靠简单的固定距离解释。现代编码还利用概率图、软信息和迭代解码。汉明球只是理解二元对称信道的一个重要几何入口。

### 8. 高斯信道容量

对带宽为 B、平均信号功率为 S、噪声功率为 N 的加性白高斯噪声信道，经典容量公式为：

```text
C = B log₂(1 + S/N)
```

这里 C 的单位为 bit/s。

该公式说明：

- 增加带宽 B 通常增加容量。
- 提高信噪比 S/N 增加容量。
- 容量对信噪比是对数增长，单纯增加发射功率存在递减收益。
- 噪声不为零时，容量仍可为正。

该公式依赖理想化的高斯噪声、带宽与平均功率约束；真实无线系统还涉及衰落、干扰、多天线、峰值功率和频谱规则。

### 9. 可靠不等于无冗余

噪声信道中的可靠通信通常需要冗余。关键不是完全消除冗余，而是以尽可能高效的方式使用冗余。

常见机制包括：

- 分组码与卷积码。
- Reed–Solomon 码。
- Turbo 码。
- LDPC 码。
- Polar 码。
- 自动重传请求与前向纠错的组合。

重复编码容易理解，但效率低。现代编码可以在接近信道容量的速率下工作，同时维持很低的错误率，不过会付出码长、计算复杂度和时延成本。

### 10. 信源编码与信道编码

信息论区分两件事：

- **信源编码**：去除可预测冗余，压缩信息，极限由信源熵控制。
- **信道编码**：主动加入结构化冗余，对抗噪声，极限由信道容量控制。

在经典条件下，信源—信道分离定理表明：若信源熵率低于信道容量，可以先最优压缩，再最优纠错，而不损失渐近最优性。

简化地写：

```text
可靠传输需要：信源信息率 < 信道容量
```

但在短码长、严格时延、多用户网络或语义失真度要求下，联合设计可能更合适。

### 11. 现实吞吐量为何低于容量

信道容量是理想统计模型下的上限。实际可用吞吐量还会被多层开销降低：

- 同步、导频与信道估计。
- 帧头、校验和协议头。
- 重传与拥塞控制。
- 编码和解码时延。
- 硬件功耗与计算能力。
- 多用户竞争与外部干扰。
- 信道统计变化和模型失配。
- 对极低错误率的额外要求。

因此，物理层容量、链路层有效速率与应用层吞吐量不是同一个量。把网速直接等同于香农容量会忽略协议栈和系统约束。

### 12. 典型应用

- **无线通信**：蜂窝网络、Wi‑Fi、卫星和深空通信的频谱与功率极限。
- **光纤通信**：分析噪声、带宽和非线性条件下的传输上限。
- **数据存储**：硬盘、闪存、光盘中的读写噪声与纠错码。
- **计算机网络**：理解物理链路上限、编码开销和有效吞吐量的区别。
- **芯片与总线**：高速串行链路在串扰、热噪声和时钟抖动下的可靠传输。
- **传感器网络**：在能量受限条件下权衡采样、编码与通信。
- **神经科学**：估计神经响应对刺激携带的互信息，但生物信道并不总满足简单无记忆模型。
- **机器学习**：信息瓶颈、表示学习和通信受限分布式学习会使用互信息与率失真思想。

### 13. 重要意义与局限

香农信道容量最深刻的结论是：噪声并不必然阻止可靠通信。只要传输速率低于容量，合适编码就能使错误概率任意小；但容量也划出一道无法靠编码技巧突破的理论边界。

它改变了通信工程的目标：

- 不再执着于制造绝对无噪声介质。
- 通过编码把物理可靠性问题转化为数学结构问题。
- 用统一指标比较不同信道与资源配置。
- 明确区分“某个方案做不到”和“任何方案都做不到”。

主要局限包括：

- 容量依赖正确的信道模型和约束。
- 渐近结论可能需要很长码字，现实系统受时延限制。
- 定理保证编码存在，不一定直接给出低复杂度构造。
- 非平稳、有记忆、多用户或对抗性信道需要更复杂理论。
- 接近容量时，编码复杂度、功耗和时延通常上升。

## 故事与理论对应

| 故事元素 | 理论对应 | 对应性质 |
| --- | --- | --- |
| 黑灯与白灯 | 二元信道输入 X ∈ {0,1} | 严格对应 |
| 雾使灯色偶尔翻转 | 二元对称信道的误码概率 p | 严格对应于理想化模型 |
| 每次错误看不出痕迹 | 接收端只观察 Y，不知道某次是否翻转 | 严格对应 |
| 单个命令重复三次 | 重复码与多数判决 | 严格对应 |
| 速度降为约三分之一 | 重复三次使编码率下降 | 严格对应，未计协议开销 |
| 六灯合法序列彼此相隔 | 码字与汉明距离 | 严格结构对应 |
| 受扰序列离某合法序列最近 | 最近邻解码 | 严格对应 |
| 把许多命令合成长块编码 | 长码字与块编码 | 严格对应 |
| 星座周围的“雾区” | 条件典型集合或噪声球 | 核心几何类比 |
| 雾区不重叠时可辨认 | 码字可可靠区分 | 结构对应；真实证明允许小概率重叠 |
| 码字过密后无法区分 | 速率超过容量时可靠解码不可能 | 核心对应 |
| 低于界线时错误可任意小 | 信道编码定理的可达性部分 | 严格对应渐近结论 |
| 高于界线时任何码都失败 | 信道编码定理的反面部分 | 严格对应 |
| 有限长度仍可能出错 | 容量定理不是零误码的有限码长保证 | 严格对应 |
| 调整黑白灯使用比例 | 对输入分布 P(X) 优化 | 严格对应 |
| 军官闪得更快却频繁重传 | 原始符号速率与有效可靠吞吐量的区别 | 工程类比 |
| 点灯延迟、能耗与查表 | 有限码长、复杂度、功耗和时延约束 | 应用类比 |
| 雾海画出不可逾越边界 | 信道容量 C | 核心概念类比 |

## 一句话总结

香农信道容量是在给定噪声模型与资源约束下，通过最优输入和编码能够实现任意低错误率的最高信息传输速率。
