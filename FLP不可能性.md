# 钟声落下之前

> 概念：FLP 不可能性（FLP Impossibility）  
> 领域：分布式计算、理论计算机科学

## 寓言故事

群岛王国没有国王，只有五座自治的灯塔。每逢风暴来临，它们必须共同决定：让所有船只驶向北港，还是驶向南港。

规则很严：五座灯塔最终必须发出同一种颜色的光；如果所有灯塔最初都赞成北港，结果不能无故变成南港；而且无论海况怎样，决定都必须在钟声落下之前作出。

灯塔之间看不见彼此，只能托信使乘小船传递意见。信使诚实，从不改动书信，却可能被浓雾困住。谁也无法判断一封迟迟未到的信究竟还在海上，还是发信的灯塔已经熄灭。

第一年，五位守塔人约定：收齐所有意见后，选择多数。风暴中东塔的火炉损坏，再也没有回信，其余四塔一直等到港口被淹。

第二年，他们改成：收到四封信就决定。北塔最先收到三票北港、一票南港，于是准备点起蓝灯；南塔收到的第四封却恰好来自另一位守塔人，看见的是两票对两票。两座灯塔依据各自已知的世界，走到了不同边缘。

第三年，他们设计了更复杂的轮次。每一轮先提议，再确认；若有人失联，就由下一座灯塔主持。起初一切顺利，直到一位信使在两座岛之间漂了整夜。他携带的只是一封很短的确认信，却让接收它之前和之后的世界有了不同的可能。

岛上的老记录员伊瑟开始整理所有可能的局面。他把必然会选北港的局面画成蓝色，把必然会选南港的局面画成红色；还有一些局面，无论最终走向哪一边都仍有可能，他就留成灰色。

他发现，最初一定存在灰色局面：否则，只改变一位守塔人的初始意见，结果就会从全蓝突然跳到全红，中间必有一步同时保留两种结局的可能。

更奇怪的是，只要局面仍是灰色，浓雾总能暂缓某一封关键书信，先让别的书信抵达，使新的局面仍保持灰色。协议没有出错，守塔人没有撒谎，已寄出的信也没有丢失；决定却可以被一次又一次推迟。

议会指责伊瑟：“你是在说，只要信使够慢，我们就永远不能决定？”

伊瑟回答：“不是任何一次延迟都会阻止决定，而是你们同时要求了三件事：永不分裂、从不决定荒谬的结果、并且在无法分辨迟到与熄灭的世界里保证按时结束。浓雾只需在每一步选择那封最不该现在到达的信，就能让最后的选择永远停留在下一步。”

后来，群岛修改了誓言。他们不再承诺在所有可能的浓雾中必然按时决定，而是引入大致同步的潮汐表、超时后的多数法定人数，以及一枚由抽签产生的幸运币。

钟声终于经常能够落在统一灯光之后，但石碑上的旧誓言被保留下来。上面写着：

> 有时，阻止共同决定的并非背叛，而是无法证明沉默意味着什么。

## 故事背后的概念：FLP 不可能性

FLP 不可能性由 Fischer、Lynch 和 Paterson 提出。它讨论的是异步消息传递系统中的确定性共识：当系统允许至少一个进程发生崩溃停止时，不存在一个确定性协议能够同时对所有合法执行保证共识的安全性与终止性。

这里的结论非常精确：

```text
系统模型：完全异步 + 可靠消息传递 + 至少允许 1 个进程崩溃停止
协议类型：确定性共识协议
结论：不能对所有合法执行同时保证安全性与必然终止
```

它并不是说分布式系统永远无法达成共识，也不是说某次执行一定无法结束。它说的是：在上述模型中，总能构造出一种合法的消息调度，使确定性协议迟迟不能作出决定。

## 共识需要满足什么

常见的二值共识包含三个性质：

- 一致性（Agreement）：所有正确进程一旦决定，决定值必须相同。
- 有效性（Validity）：决定值必须与参与者的合法输入有关；例如所有输入都是 0 时不能决定 1。
- 终止性（Termination）：每个正确进程最终都必须作出决定。

FLP 的锋利之处在于：即使只有一个进程可能崩溃、消息不会损坏也不会永久丢失，只要消息延迟没有已知上界，就无法保证三者在所有执行中同时成立。

## 二价、单价与关键一步

证明把全局状态分为三类：

```text
0-valent：从该状态继续，最终只能决定 0
1-valent：从该状态继续，最终只能决定 1
bivalent：从该状态继续，0 和 1 都仍可能成为结果
```

证明的核心结构是：

1. 至少存在一个初始二价状态。
2. 从二价状态出发，对某个待处理事件，可以安排其他事件先发生，使系统仍保持二价。
3. 反复选择这样的调度，就能构造一条无限执行路径；系统一直合法运行，却始终没有跨入任何单价决定状态。

直觉上，异步系统无法可靠区分“某节点已经崩溃”与“它只是非常慢”。如果协议必须等待它，终止性可能失败；如果不等待它，在某些信息分布下又可能威胁一致性或有效性。

## 为什么现实系统仍然能够工作

工程系统通常通过改变 FLP 的某个前提来取得进展：

- 部分同步：假设系统最终进入消息延迟有界的时期。
- 故障检测器：提供对失效节点最终趋于准确的怀疑信息。
- 随机化：使用随机选择，使对手无法永远精确安排阻塞决定的下一步。
- 法定人数：在多数节点可用时推进，同时依靠交集性质保护安全性。
- 区分安全与活性：网络分区时优先保证安全，允许系统暂时停止推进。

Paxos、Raft 等协议并没有推翻 FLP。它们通常在安全性上提供严格保证，而活性依赖领导者最终稳定、超时设置有效、网络恢复到足够同步等条件。

## 典型应用与意义

- 理解分布式数据库为什么会在网络异常时卡住，而不是冒险提交冲突结果。
- 分析共识协议中的选主、超时、重试和法定人数设计。
- 区分“协议通常能完成”与“协议在所有合法调度下保证完成”。
- 识别系统文档中隐藏的同步性、故障模型与活性假设。
- 理解安全性和活性为何必须分别声明、分别证明。

## 故事与理论的对应

| 故事元素 | 理论对应 | 对应性质 |
| --- | --- | --- |
| 五座自治灯塔 | 多个分布式进程或节点 | 严格对应 |
| 北港与南港 | 二值共识中的 0 与 1 | 严格对应 |
| 所有灯塔发出同色光 | 一致性 | 严格对应 |
| 全部赞成北港时不能决定南港 | 有效性 | 严格对应 |
| 钟声前必须作出决定 | 终止性 | 严格对应 |
| 信使诚实但可能任意迟到 | 可靠但延迟无上界的异步消息 | 严格对应 |
| 无法区分迟到与灯塔熄灭 | 异步系统无法完美识别崩溃 | 严格对应 |
| 蓝色、红色、灰色局面 | 0 价、1 价与二价状态 | 严格对应 |
| 暂缓关键书信仍保持灰色 | 对消息事件的对抗性调度 | 严格对应 |
| 幸运币 | 随机化共识 | 近似对应 |
| 潮汐表与超时 | 部分同步和故障检测假设 | 近似对应 |
| 浓雾像有意选择消息顺序 | 证明中的调度对手，不是现实中的实体敌人 | 类比 |

## 一句话总结

FLP 不可能性告诉我们：在完全异步且允许节点崩溃的系统中，确定性共识无法对所有合法执行同时保证正确决定与必然结束；现实协议之所以能前进，是因为它们明确或隐含地增加了额外假设。
